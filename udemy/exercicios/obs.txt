No css
* = seletor universal, todas as tags são afetadas pelo css
. é utilizado para classes
seletor por atributo é feito com [type="(o tipo da variavel)"]
seletor pseudos é feito com :, :houver significa q quando o mouse estiver passando por um elemento tem a seguinte ação
seletor por ID é feito com #.
.content-center img, nesse caso estou falando a classe pra centralizar mas em foco na tag toma tal decisão
border-radius é para fazer uma borda arredondada
outline é para tirar as linhas de uma borda

let stringjson essa variavel transforma o objeto em string = JSON.stringify essa e a funcao que transforma(json);
    console.log(stringjson);
    let jsonParse = JSON.parseessa funcao transforma a string num jsonobject(stringjson);
    console.log(jsonParse);